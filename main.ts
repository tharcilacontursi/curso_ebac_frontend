const multiplicaNumeros = (numeroMultiplicado: number , numeroMultiplicador: number) => numeroMultiplicado * numeroMultiplicador;

const multiplicaNumeros2 = multiplicaNumeros(100,2);
console.log(multiplicaNumeros2)


const saudacao = (nome:string) => "Olá " + nome; 

const nomesaudacao = saudacao('Ana');
console.log(nomesaudacao);